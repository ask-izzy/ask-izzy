import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="MDX/Checkbox" />

# Checkbox

With `MDX`, we can define a story for `Checkbox` right in the middle of our Markdown documentation.

<Canvas>
    <div>
        <p style={{fontSize: 'var(--type-size-0)'}}>
            Type size 0
        </p>
        <p style={{fontSize: 'var(--type-size-1)'}}>
            Type size 1
        </p>
        <p style={{fontSize: 'var(--type-size-2)'}}>
            Type size 2
        </p>
        <p style={{fontSize: 'var(--type-size-3)'}}>
            Type size 3
        </p>
    </div>
    <div>
        <p style={{fontSize: 'var(--type-size-0)', fontWeight: 'bold'}}>
            Type size 0
        </p>
        <p style={{fontSize: 'var(--type-size-1)', fontWeight: 'bold'}}>
            Type size 1
        </p>
        <p style={{fontSize: 'var(--type-size-2)', fontWeight: 'bold'}}>
            Type size 2
        </p>
        <p style={{fontSize: 'var(--type-size-3)', fontWeight: 'bold'}}>
            Type size 3
        </p>
    </div>
</Canvas>
